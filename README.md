# FWT Art Gallery

Веб-приложение для управления галереей художников и их произведений искусства. Приложение позволяет просматривать, добавлять, редактировать и удалять информацию о художниках и их картинах.

## 🎨 Основные возможности

- **Управление художниками**
  - Просмотр списка художников с пагинацией
  - Добавление новых художников с загрузкой аватара
  - Редактирование информации о художниках
  - Удаление художников
  - Просмотр профиля художника с галереей его картин

- **Управление картинами**
  - Просмотр картин в галерее
  - Добавление картин к художнику
  - Редактирование информации о картинах
  - Удаление картин
  - Слайдер для просмотра картин

- **Фильтрация и поиск**
  - Поиск художников по имени
  - Фильтрация по жанрам
  - Сортировка по различным параметрам (имя, дата создания и т.д.)

- **Аутентификация**
  - Регистрация новых пользователей
  - Вход в систему
  - Защищенные маршруты

- **Интерфейс**
  - Темная и светлая тема
  - Адаптивный дизайн (mobile-first)
  - Модальные окна для различных действий
  - Toast-уведомления
  - Скелетоны загрузки
  - Обработка ошибок

## 🛠 Технологический стек

### Основные технологии

- **React 19** — библиотека для создания пользовательского интерфейса
- **TypeScript** — типизированный JavaScript
- **Vite** — сборщик и dev-сервер
- **React Router DOM** — маршрутизация в приложении

### Управление состоянием

- **Redux Toolkit** — управление глобальным состоянием
- **React Hook Form** — управление формами
- **Yup** — валидация форм

### Стилизация

- **SCSS/SASS** — препроцессор CSS
- **CSS Modules** — модульные стили

### Дополнительные библиотеки

- **Axios** — HTTP-клиент для работы с API
- **FingerprintJS** — идентификация устройств
- **React Error Boundary** — обработка ошибок React
- **clsx** — условное объединение классов
- **nanoid** — генерация уникальных ID

### Инструменты разработки

- **ESLint** — линтер для JavaScript/TypeScript
- **Prettier** — форматирование кода
- **Husky** — Git hooks
- **Storybook** — документация и разработка компонентов
- **Vitest** — фреймворк для тестирования
- **Playwright** — E2E тестирование

## 📦 Установка и запуск

### Предварительные требования

- Node.js (версия 18 или выше)
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`

### Просмотр production сборки

```bash
npm run preview
```

### Запуск линтера

```bash
npm run lint
```

### Форматирование кода

```bash
npm run prettier
```

### Запуск тестов

```bash
npm run vitest
```

### Запуск Storybook

```bash
npm run storybook
```

Storybook будет доступен по адресу `http://localhost:6006`

### Сборка Storybook

```bash
npm run build-storybook
```

## 📁 Структура проекта

```
fwt-art-gallery/
├── src/
│   ├── components/          # React компоненты
│   │   ├── Artist/         # Компонент художника
│   │   ├── ArtistModal/    # Модальное окно добавления/редактирования художника
│   │   ├── AuthModal/      # Модальное окно авторизации
│   │   ├── RegisterModal/  # Модальное окно регистрации
│   │   ├── Filter/         # Компонент фильтрации
│   │   ├── Pagination/     # Компонент пагинации
│   │   ├── PaintingGallery/# Галерея картин
│   │   ├── PaintModal/     # Модальное окно добавления/редактирования картины
│   │   ├── Header/         # Шапка сайта
│   │   ├── Footer/         # Подвал сайта
│   │   └── ...
│   ├── pages/              # Страницы приложения
│   │   ├── MainPage/       # Главная страница со списком художников
│   │   └── ArtistProfile/  # Страница профиля художника
│   ├── store/              # Redux store
│   │   ├── api/           # RTK Query API endpoints
│   │   └── slices/        # Redux slices
│   ├── ui_kit/            # UI компоненты (кнопки, инпуты, карточки и т.д.)
│   ├── hooks/             # Кастомные React хуки
│   ├── Context/           # React Context провайдеры
│   ├── types/             # TypeScript типы и интерфейсы
│   ├── utils/             # Утилиты
│   ├── styles/            # Глобальные стили
│   ├── assets/            # Статические ресурсы (изображения, иконки)
│   ├── fonts/             # Шрифты
│   ├── App.tsx            # Главный компонент приложения
│   └── main.tsx           # Точка входа
├── public/                # Публичные файлы
├── .storybook/            # Конфигурация Storybook
├── dist/                  # Собранные файлы (после build)
├── package.json           # Зависимости и скрипты
├── vite.config.ts         # Конфигурация Vite
├── tsconfig.json          # Конфигурация TypeScript
└── README.md              # Документация
```

## 🎯 Основные функции

### Аутентификация

- Регистрация новых пользователей с валидацией формы
- Вход в систему с сохранением токенов
- Защита маршрутов для авторизованных пользователей
- Автоматическое обновление токенов

### Управление художниками

- CRUD операции для художников
- Загрузка аватара художника
- Указание жанров, годов жизни, местоположения
- Описание художника

### Управление картинами

- Добавление картин к художнику
- Загрузка изображений картин (поддержка WebP)
- Редактирование названия и года создания
- Удаление картин
- Слайдер для просмотра картин

### Фильтрация и сортировка

- Поиск по имени художника
- Фильтрация по жанрам (мультиселект)
- Сортировка по имени, дате создания
- Направление сортировки (по возрастанию/убыванию)

### UI/UX

- Переключение между темной и светлой темой
- Адаптивный дизайн для мобильных устройств
- Модальные окна для различных действий
- Toast-уведомления для обратной связи
- Скелетоны во время загрузки данных
- Обработка ошибок с понятными сообщениями

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта для настройки API endpoints:

```env
VITE_API_URL=your_api_url_here
```

### Алиасы путей

В проекте настроены алиасы для удобного импорта:

- `@/` — указывает на директорию `src/`

Пример использования:

```typescript
import { useGetArtistsQuery } from '@/store/api/artistsApi';

import Button from '@/ui_kit/Buttons';
```

## 🧪 Тестирование

Проект использует Vitest для unit-тестов и Playwright для E2E тестов. Тесты можно запустить командой:

```bash
npm run vitest
```

## 📝 Форматирование кода

Проект использует Prettier для автоматического форматирования кода. Перед коммитом код автоматически форматируется через Husky hooks.

Ручное форматирование:

```bash
npm run prettier
```

## 🤝 Вклад в проект

1. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
2. Внесите изменения и закоммитьте (`git commit -m 'Add some AmazingFeature'`)
3. Запушьте в ветку (`git push origin feature/AmazingFeature`)
4. Откройте Pull Request

## 📄 Лицензия

Этот проект является приватным.

## 👥 Авторы

Разработано командой FWT.

---

Для дополнительной информации или вопросов, пожалуйста, создайте issue в репозитории проекта.
